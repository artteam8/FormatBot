Telegram has added rich formatting in recent update, but requires premium subscription to format your messages.

However, bots support formatting even when their owner doesn't have Premium. This is a format echo bot that renders math Latex in your messages; currently, functionality is very basic and may be extended to full editor in the future.

An example: 
\forall \varepsilon > 0 \\; \exists \delta(\varepsilon) > 0: \forall x \in B_{\delta}(x_0): |x - x_0| < \delta \Rightarrow |f(x) - f(x_0)| < \varepsilon -> $\forall \varepsilon > 0 \\; \exists \delta(\varepsilon) > 0: \forall x \in B_{\delta}(x_0): |x - x_0| < \delta \Rightarrow |f(x) - f(x_0)| < \varepsilon$
